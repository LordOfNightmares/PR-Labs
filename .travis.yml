language: python
python:
  - "3.5-dev"  # 3.5 development branch
  - "3.6"
  - "3.6-dev" # 3.6 development branch
  - "3.7-dev"  # 3.7 development branch
  - "nightly"
# command to install dependencies
install:
  - pip install -r unittest2
# command to run tests
script: python PythonTestLab.py
